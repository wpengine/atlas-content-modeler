import {
	sendEvent,
	sendPageView,
	isExcluded,
	shouldTrack,
	telemetryEnabled,
} from "../../../includes/shared-assets/js/analytics";

test("telemetry", () => {});

describe("analytics", () => {
	test("", () => {});
});
